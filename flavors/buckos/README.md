# BuckOS flavor

The BuckOS flavor is declared but not implemented.

`defs/flavor.bzl` accepts `buckos` as a known flavor name and fails package loading with an implementation-status message. This repository does not define a BuckOS buildroot, package frontend, external cell, generated package data, or image targets.
